---
title: Learning Fp8LinearMethod
date: 2026-05-25
topics: [vllm]
---

# Learning Fp8LinearMethod

## Deepseek V4 运行过程

1. 模型初始化定义了 ColumnParallelLinear (在attention的初始化里面)
TODO 
    其他非 Parallel的权重如何处理？
    什么是MergedColumnParallelLinear？
2. ColumnParallelLinear初始化
    1. 计算TP的对应的数值
    2. 父类初始化：
        1. 获取量化方法
    3. 调用 quant_method.create_weights() 创建参数，注册了loader方法
    4. 处理bias
    5. 更新tp参数
3. ColumnParallelLinear加载权重
    1. 在模型加载权重的时候遍历加载调用之前注册的loader方法
4. ColumnParallelLinear forward
    1. self.quant_method.apply里面有一些dispatch的方式
    2. Fp8BlockScaledMMLinearKernel apply_weights reshape 处理input量化 调用 GEMM kernel

## Fp8LinearMethod 初始化
1. 保存基本配置，量化配置，算子支持，数据type
2. 检查deepgemm
3. 判断量化类型是 per-tensor/block/token
4. 创建量化key （create weight的时候用key来创建）


## Fp8LinearMethod create_weights

1. 保存一些tp信息
2. 创建了一个ModelWeightParameter
3. 创建scale参数
4. 选择FP8 kernel保存在self.fp8_linear

## Fp8LinearMethod process_weights_after_loading
1. 其实就是一个hook可以处理加载好的权重

## Fp8LinearMethod apply

1. 其实就是dispatch一下kernel 包含 self.fp8_linear

## 关于PRxxx的问题

1. 其实就是这个method进行了更新 其实增加了一切额外的隐限制 但是没有检查和fallback